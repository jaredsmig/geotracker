version: '3.8'
services:
  api:
    build: ./api
    ports:
      - "5000:5000"
    environment:
      - ASPNETCORE_URLS=http://+:5000
  web:
    build: ./web
    ports:
      - "3000:80"
    depends_on:
      - api
